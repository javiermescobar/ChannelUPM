<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/layout_background"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/error_dialog_button_width"
    android:background="@drawable/rounded_category_unselected"
    android:layout_marginStart="@dimen/smallest_margin"
    android:layout_marginEnd="@dimen/smallest_margin">

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/category_button"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/transparent"/>

    <TextView
        android:id="@+id/title_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="Título de categoría"
        android:gravity="center"
        android:textSize="@dimen/normal_text"
        style="@style/category_text"/>

</androidx.constraintlayout.widget.ConstraintLayout>