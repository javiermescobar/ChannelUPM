<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/dialog_background">

        <ImageView
            android:id="@+id/informaiton_image"
            android:layout_width="@dimen/big_margin"
            android:layout_height="@dimen/big_margin"
            android:src="@drawable/ic_warning"
            android:layout_gravity="center"/>

        <TextView
            android:id="@+id/information_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/normal_text"
            style="@style/default_text"
            android:text="Mensaje de información"
            android:layout_margin="@dimen/small_margin"
            android:gravity="center"/>

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/information_confirmation_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/rounded_button"
            android:gravity="center"
            android:text="@string/error_confirm"
            style="@style/default_text"
            android:textSize="@dimen/small_text"
            android:layout_margin="@dimen/small_margin"/>

    </LinearLayout>
</layout>